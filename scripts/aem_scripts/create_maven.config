#!/usr/bin/env bash

USERNAME="danofer"
PASSWORD="foobar"

echo "--settings=./.mvn/settings.xml" > maven.config
echo "-Dusername=${USERNAME}" >> maven.config
echo "-Dpassword=${PASSWORD}" >> maven.config
